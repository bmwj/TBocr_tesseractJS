version: "3.8"

services:
  tbocr-web:
    build: .
    container_name: tbocr-tesseractjs
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped

